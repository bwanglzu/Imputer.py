deployment:
  staging:
    branch: dev
machine:
  python:
    version: 2.7.12
test:
  override:
    - nosetests --with-coverage
  post:
    - "flake8 ./ --exclude __init__.py,build"
    - coverage xml
    - codecov